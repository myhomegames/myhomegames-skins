# Optional: GitHub Personal Access Token (classic) with "repo" scope for npm run release.
# Without it, release-it opens the GitHub web UI (no automatic asset upload).
# Create at https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_your_token_here

# Optional: server metadata directory for `npm run sync:metadata` / `npm run build`.
# Same path as myhomegames-server METADATA_PATH. MHG_METADATA_PATH wins if both are set.
# METADATA_PATH=/path/to/your/metadata
# MHG_METADATA_PATH=/path/to/your/metadata
